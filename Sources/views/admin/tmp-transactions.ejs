<%- include('../partials/head') %>
<%- include('../partials/nav') %>

<div class="section" style="min-height: 80vh;">
  <div class="container admin-layout">
    <%- include('../partials/admin-sidebar', { currentPage: currentPage || 'tmp-transactions' }) %>

    <div class="admin-content">
      <h3>Danh sách giao dịch tạm</h3>
      <table class="admin-table">
        <thead>
          <tr>
            <th>Mã GD tạm</th>
            <th>Khách hàng</th>
            <th>Phim</th>
            <th>Phòng</th>
            <th>Ghế</th>
            <th>Giờ chiếu</th>
            <th>Trạng thái</th>
            <th>Ngày tạo</th>
            <th>Hết hạn</th>
          </tr>
        </thead>
        <tbody>
          <% transactions.forEach(t => { %>
            <tr>
              <td><%= t.MaGiaoDichTmp %></td>
              <td><%= t.TenKH %></td>
              <td><%= t.TenPhim %></td>
              <td><%= t.TenPhong %></td>
              <td><%= t.GheNgoi %></td>
              <td><%= t.GioChieu %></td>
              <td><%= t.TrangThai %></td>
              <td><%= t.NgayTao %></td>
              <td><%= t.HetHan %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<%- include('../partials/footer', { footerMenus: [] }) %>
<%- include('../partials/scripts') %>

<link rel="stylesheet" href="/css/admin-premium.css">
<style>
  .admin-content h3 {
    color: var(--main-color);
    font-size: 1.8rem;
    margin-bottom: 1.5rem;
  }
</style>

