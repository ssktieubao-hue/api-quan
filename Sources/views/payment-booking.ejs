<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Thanh toán vé xem phim</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .payment-container {
      background: white;
      border-radius: 12px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
      padding: 40px;
      max-width: 600px;
      width: 100%;
    }

    h1 {
      color: #333;
      margin-bottom: 10px;
      font-size: 28px;
    }

    .subtitle {
      color: #666;
      margin-bottom: 30px;
      font-size: 16px;
    }

    .ticket-summary {
      background-color: #f5f5f5;
      padding: 20px;
      border-radius: 8px;
      margin-bottom: 30px;
    }

    .summary-row {
      display: flex;
      justify-content: space-between;
      padding: 10px 0;
      border-bottom: 1px solid #ddd;
      font-size: 14px;
    }

    .summary-row:last-child {
      border-bottom: none;
    }

    .summary-label {
      color: #666;
      font-weight: 500;
    }

    .summary-value {
      color: #333;
      font-weight: bold;
    }

    .summary-row.total {
      background-color: #fff;
      padding: 12px;
      margin-top: 10px;
      border-radius: 4px;
      font-weight: bold;
      font-size: 18px;
      color: #2196f3;
    }

    .payment-methods {
      margin-bottom: 30px;
    }

    .payment-methods h3 {
      color: #333;
      margin-bottom: 15px;
      font-size: 16px;
    }

    .method-option {
      display: flex;
      align-items: center;
      padding: 15px;
      border: 2px solid #ddd;
      border-radius: 8px;
      margin-bottom: 10px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .method-option:hover {
      border-color: #2196f3;
      background-color: #f9f9f9;
    }

    .method-option input[type="radio"] {
      margin-right: 15px;
      cursor: pointer;
      width: 18px;
      height: 18px;
    }

    .method-info {
      flex: 1;
    }

    .method-name {
      font-weight: bold;
      color: #333;
      font-size: 15px;
    }

    .method-desc {
      color: #999;
      font-size: 12px;
      margin-top: 3px;
    }

    .action-buttons {
      display: flex;
      gap: 10px;
      justify-content: center;
      flex-wrap: wrap;
    }

    .btn {
      padding: 12px 24px;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-block;
    }

    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      flex: 1;
      min-width: 150px;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
    }

    .btn-primary:disabled {
      background: #ccc;
      cursor: not-allowed;
      transform: none;
    }

    .btn-secondary {
      background-color: #e0e0e0;
      color: #333;
      flex: 1;
      min-width: 150px;
    }

    .btn-secondary:hover {
      background-color: #d0d0d0;
    }

    .loading {
      display: none;
      text-align: center;
      padding: 20px;
      color: #666;
    }

    .spinner {
      border: 4px solid #f3f3f3;
      border-top: 4px solid #2196f3;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      animation: spin 1s linear infinite;
      margin: 0 auto 10px;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    @media (max-width: 600px) {
      .payment-container {
        padding: 30px 20px;
      }

      h1 {
        font-size: 24px;
      }

      .action-buttons {
        flex-direction: column;
      }

      .btn {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="payment-container">
    <h1>Thanh toán MoMo (Demo)</h1>
    <p class="subtitle">Vé của bạn đã được đặt tạm thời. Vui lòng quét mã QR MoMo để hoàn tất (demo, không trừ tiền thật).</p>

    <!-- Tóm tắt vé -->
    <div class="ticket-summary">
      <div class="summary-row">
        <span class="summary-label">Số ghế:</span>
        <span class="summary-value" id="seatsDisplay">-</span>
      </div>
      <div class="summary-row">
        <span class="summary-label">Số lượng:</span>
        <span class="summary-value" id="seatsCountDisplay">0</span>
      </div>
      <div class="summary-row">
        <span class="summary-label">Đơn giá:</span>
        <span class="summary-value" id="priceDisplay">0 VNĐ</span>
      </div>
      <div class="summary-row total">
        <span>Tổng tiền:</span>
        <span id="totalDisplay">0 VNĐ</span>
      </div>
    </div>

    <!-- Mã QR MoMo -->
    <div class="payment-methods">
      <h3>Quét mã QR MoMo</h3>
      <div style="display:flex;flex-direction:column;align-items:center;gap:1rem;">
        <div style="background:#fff;padding:12px;border-radius:12px;box-shadow:0 4px 20px rgba(0,0,0,0.1);">
          <!-- Đổi lại tên file QR theo file của bạn trong /public/images/payment -->
          <img src="/images/payment/QR-MOMO.png" alt="Mã QR MoMo" style="width:260px;height:260px;object-fit:contain;">
        </div>
        <p style="color:#666;font-size:14px;text-align:center;max-width:360px;">
          Mở ứng dụng MoMo &gt; Chọn tính năng <strong>Quét mã</strong> &gt; quét QR trên màn hình.
          Đây chỉ là demo, bạn có thể bỏ qua bước thanh toán thật và bấm nút xác nhận bên dưới.
        </p>
      </div>
    </div>

    <!-- Nút hành động -->
    <div class="action-buttons">
      <button onclick="goBack()" class="btn btn-secondary">Quay lại</button>
      <button onclick="finishDemoPayment()" class="btn btn-primary" id="paymentBtn">Tôi đã thanh toán (Demo)</button>
    </div>
  </div>

  <script>
    // Lấy thông tin vé từ sessionStorage
    function loadTicketInfo() {
      const ticketInfo = sessionStorage.getItem('ticketInfo');
      
      if (!ticketInfo) {
        alert('Không tìm thấy thông tin vé. Vui lòng đặt vé lại.');
        window.location.href = '/';
        return;
      }

      try {
        const info = JSON.parse(ticketInfo);
        const { selectedSeats, totalAmount, averagePrice } = info;

        // Hiển thị thông tin
        document.getElementById('seatsDisplay').textContent = selectedSeats.sort().join(', ');
        document.getElementById('seatsCountDisplay').textContent = selectedSeats.length;
        
        // Hiển thị đơn giá (giá trung bình)
        document.getElementById('priceDisplay').textContent = 
          new Intl.NumberFormat('vi-VN', {
            style: 'currency',
            currency: 'VND',
          }).format(averagePrice || 0);
        
        // Hiển thị tổng tiền
        document.getElementById('totalDisplay').textContent = 
          new Intl.NumberFormat('vi-VN', {
            style: 'currency',
            currency: 'VND',
          }).format(totalAmount);

        // Lưu MaLich và totalAmount để dùng khi thanh toán
        window.MaLich = info.MaLich;
        window.totalAmount = totalAmount;
      } catch (error) {
        console.error('Lỗi parse ticketInfo:', error);
        alert('Có lỗi xảy ra. Vui lòng đặt vé lại.');
        window.location.href = '/';
      }
    }

    // Thanh toán demo với MoMo: gọi API demo-complete để tạo vé rồi redirect
    async function finishDemoPayment() {
      if (!window.MaLich || !window.totalAmount) {
        alert('Thông tin vé không hợp lệ, vui lòng đặt lại.');
        window.location.href = '/';
        return;
      }

      const ticketInfoStr = sessionStorage.getItem('ticketInfo');
      if (!ticketInfoStr) {
        alert('Không tìm thấy thông tin vé, vui lòng đặt lại.');
        window.location.href = '/';
        return;
      }

      const info = JSON.parse(ticketInfoStr);
      const orderId = info.orderId || ('DEMO-' + Date.now());
      info.orderId = orderId;
      sessionStorage.setItem('ticketInfo', JSON.stringify(info));

      try {
        const res = await fetch('/api/payment/demo-complete', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ orderId }),
        });
        const data = await res.json();
        if (!res.ok || !data.success) {
          alert(data.message || 'Không thể hoàn tất thanh toán, nhưng đây là demo nên bạn vẫn có thể kiểm tra vé.');
        }
      } catch (e) {
        console.error('demo-complete error', e);
      }

      window.location.href = '/payment-success?orderId=' + encodeURIComponent(orderId);
    }

    // Xử lý khi người dùng ấn "Quay lại" - hiển thị thông báo ghế tạm giữ
    function goBack() {
      const ticketInfo = sessionStorage.getItem('ticketInfo');
      if (ticketInfo) {
        try {
          const info = JSON.parse(ticketInfo);
          const { selectedSeats, orderId } = info;
          
          if (selectedSeats && selectedSeats.length > 0) {
            const seatsList = selectedSeats.sort().join(', ');
            alert(`Ghế ${seatsList} đang tạm giữ (chưa thanh toán).\nBạn có thể quay lại để thanh toán sau.`);
          }
        } catch (e) {
          console.error('Lỗi parse ticketInfo:', e);
        }
      }
      
      // Quay lại trang trước
      window.history.back();
    }

    // Load thông tin vé khi trang load
    document.addEventListener('DOMContentLoaded', loadTicketInfo);
  </script>
</body>
</html>
